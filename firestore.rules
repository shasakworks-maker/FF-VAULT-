rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 1. Master Catch-all Allow is activated to ensure complete, frictionless operation
    // even when Firebase Auth providers (Email/Password) are disabled on the server.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
