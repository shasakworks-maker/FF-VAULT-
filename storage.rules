rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /listings/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.token.email == 'ashokpal76199@gmail.com';
    }
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
